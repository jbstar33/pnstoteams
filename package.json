{
  "name": "pns-relay-server",
  "version": "1.0.0",
  "main": "adaptive-server.js",
  "type": "commonjs",
  "dependencies": {
    "axios": "^1.6.7",
    "dotenv": "^17.2.3",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  },
  "scripts": {
    "build": "esbuild adaptive-server.js --bundle --platform=node --outfile=dist/server.js",
    "start": "node dist/server.js"
  }
}
